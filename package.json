{
  "name": "hello-world-js",
  "version": "1.0.0",
  "description": "Hello World Node.js application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"No tests yet\" && exit 0",
    "check": "node -e \"const http=require('http');http.get('http://localhost:3000/health',r=>{console.log('OK:',r.statusCode);process.exit(r.statusCode===200?0:1)}).on('error',e=>{console.error('FAIL',e.message);process.exit(1)})\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "author": "kolluriarunakumari",
  "license": "MIT"
}